{
  "name": "gha-scripts",
  "version": "1.0.0",
  "description": "GitHub Actions scripts for tag generation",
  "main": "generate-tag.js",
  "scripts": {
    "test": "jest",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage"
  },
  "devDependencies": {
    "jest": "^29.0.0"
  },
  "jest": {
    "testEnvironment": "node",
    "collectCoverageFrom": [
      "*.js",
      "!*.test.js"
    ]
  }
}
